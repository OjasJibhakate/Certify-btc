"""
datasets.py  —  Phase 1 (not implemented yet).

Planned: a flexible multi-dataset loader that maps each dataset's native labels
onto the canonical config.CLASS_NAMES order and can combine datasets listed in
config.ACTIVE_DATASETS. Honors config.LIMIT_SAMPLES for fast local smoke tests.
"""
