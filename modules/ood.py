"""
ood.py  —  Phase 6 (not implemented yet).

Planned: out-of-distribution detection via energy score (config.OOD_METHOD) so
non-brain-MRI or garbage inputs get rejected instead of confidently misclassified.
"""
