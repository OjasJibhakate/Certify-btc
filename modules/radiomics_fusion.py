"""
radiomics_fusion.py  —  Phase 4 (not implemented yet).

Planned: PyRadiomics (107 features) computed on the pseudo-masked region -> PCA
to 32 dims -> concatenated with deep features before the final classifier.
Heavy: gated by config.HEAVY_MODULES (cloud only on full data).
"""
